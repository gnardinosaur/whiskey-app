import React from 'react';

function SubmissionResults(props) {

  debugger;

  return (
    <div>
      submission results.
      {/* <p>{props.payload}</p> */}
    </div>
  )
};

export default SubmissionResults;